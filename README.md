PesaPlan Rebuild — full project scaffold with Learning Hub and tools.
Quick start:
1. npm i
2. cp .env.example .env.local and fill keys
3. npx prisma generate
4. npx prisma migrate dev --name init
5. npm run seed
6. npm run dev
If you hit errors, paste the terminal output here and I'll patch them.
